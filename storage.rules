rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Permitir lectura y escritura pública en desarrollo y sandbox para evitar errores de autorización (storage/unauthorized)
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
